<h1>Books</h1>
<a href="/books/form">Create a new book</a>

<ul>
  {{#each books}}
    <li>{{@index}} - {{this.title}} - {{this.publishingYear}} - {{this.genreId}}</li>
    <a href="/books/edit?id={{@index}}">edit</a>
  {{/each}}
</ul>